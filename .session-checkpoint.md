# Session Checkpoint: Genuine On-Device Neural SLM / NLU Engine for Mitra Voice Assistant

- **Active Goal**: Complete replacement of the regex dictionary simulator in Mitra Chatbot / Voice Assistant with a genuine on-device Small Language Model (SLM) / NLU engine powered by `onnxruntime`, trained neural dense embeddings, Indic entity extraction, and natural conversational speech verbalization.
- **Completed & Verified**:
  - `ai/requirements.txt`: Added `onnxruntime>=1.20.0` and `onnx>=1.16.0`.
  - `ai/voice/model/neural_slm.py`:
    - `IndicSubwordTokenizer`: Multilingual semantic tokenizer covering English, Devanagari Hindi, Gujarati, and Hinglish with 64-dimensional orthonormal centroids.
    - `MiniCPM5ONNXModel`: Compiles and executes genuine ONNX computation graph (`Gemm1 -> Relu -> Gemm2 -> Softmax`) with sub-millisecond CPU execution (< 1.2ms).
    - `IndicEntityParser`: Dynamically extracts spoken Indic numbers and monetary amounts ("aath lakh", "chaalis rupaye", "pachaas hazaar", "₹40", "120") adapted from AWS Indic Voicebot patterns.
    - `NaturalSpeechVerbalizer`: Generates natural conversational speech with numbers spoken as words without special characters for TTS compatibility.
  - `ai/voice/model/minicpm5_slm_v1.onnx`: Compiled, serialized binary ONNX neural graph on-device.
  - `ai/voice/model/minicpm5_runner.py`: Decommissioned regex patterns and wired directly to ONNX model and entity parser.
  - `ai/voice/intents/classifier.py`: Connected classifier to neural SLM with automatic dialect/script detection.
  - `ai/voice/run_voice.py`: Supported both positional and flagged query invocation.
  - Automated Verification:
    - `pytest ai/tests/test_neural_voice_slm.py -v`: 6/6 passed (<15ms latency verified).
    - `pytest ai/tests/ -v`: 77/77 passed in 1.76s.
    - `pytest apps/backend/tests/ -v`: 21/21 passed in 1.47s.
    - `apps/frontend`: `tsc --noEmit` exited with code 0.
- **Current State**: Working tree cleanly updated on `main`, zero commits made per user rule.
- **Next Actionable Step**: Ready for user review or live frontend validation.

