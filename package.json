{
  "name": "abc-bank",
  "version": "1.0.0",
  "private": true,
  "description": "ABC Bank (VZEYA) — Database Tools & Prisma Studio",
  "scripts": {
    "db:studio": "dotenv -c -- prisma studio",
    "db:introspect": "dotenv -c -- prisma db pull",
    "db:validate": "prisma validate"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prisma": "^6.4.1"
  },
  "dependencies": {
    "@prisma/client": "^6.19.3"
  }
}
